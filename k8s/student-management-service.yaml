apiVersion: v1
kind: Service
metadata:
  name: student-management-app
  namespace: devops
spec:
  selector:
    app: student-management-app
  ports:
    - protocol: TCP
      port: 8089
      targetPort: 8089
  type: NodePort
